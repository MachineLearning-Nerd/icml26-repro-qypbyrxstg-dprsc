.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env
hf-cache/
# The exact Airleave/DPRSC source and datasets are vendored under upstream/ so
# fresh-clone experiment runs do not depend on a moving network checkout.
# logbook source is published to the HF Space (DineshAI/QYpByrxSTg)
.trackio/
# Local PDF-extraction scratch copied during early release builds; primary
# source URLs and hashes remain in the committed manifest.
space_candidate/evidence/claim-2/primary_sources/
