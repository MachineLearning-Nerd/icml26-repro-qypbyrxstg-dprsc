# Pinned upstream source

This directory is a byte-for-byte source and data snapshot of
[`Airleave/DPRSC`](https://github.com/Airleave/DPRSC) at commit
`aae89538544bddb1bc89961453f3cd6b6091de19`.

It is vendored because OpenResearch runs clone only the experiment branch tip.
The previous repository ignored this directory, so its documented reproduction
command could not run from a fresh clone.
