# Claim 2 method

The source verifier checks every domain and regime marker. The negative control replaces \(\widetilde{\mathrm{HS}}\) with GS and drops the success, query-size, and infinitely-many quantifiers; it must exit nonzero.

The proof route will reconstruct the discrepancy lemma, graph reduction, deterministic decoder, post-processing step, and reconstruction lower bound. Empirical dimension sweeps are not accepted as proof.
